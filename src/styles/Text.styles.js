import { makeStyles } from '@material-ui/core';

const useStylesInput = makeStyles({
  root: {
    fontSize: 'x-large',
    color: '#F2F2F2',
    textAlign: 'center',
    paddingTop: '24px'
  },
});

export default useStylesInput;